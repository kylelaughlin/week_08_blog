<h1>Users</h1>

<% @users.each do |user| %>
  <p><%= link_to user.name, user_path(user.id) %></p>
  <p><%= truncate(user.bio, length: 50) %></p>
<% end %>

<%= link_to "Create New User", :new_user %>
